/*
 * @file
 * @brief condvar(9) shell: the type and entry points live with the
 * mutex shell, which implements the pair together (see sys/mutex.h).
 */

#ifndef _COMPAT_SYS_CONDVAR_H_
#define _COMPAT_SYS_CONDVAR_H_

#include <sys/mutex.h>

#endif /* _COMPAT_SYS_CONDVAR_H_ */
